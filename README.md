# File-Packer-Unpacker-
Based on file system using Java

This project is used to perform packing and unpacking activities for multiple types of files.
In the case of the Packing activity maintain one file which contains metadata and data of multiple files
from the specified directory. In the case of the Unpacking activity, we extract all data from packed files
and according to their metadata, create all files.
